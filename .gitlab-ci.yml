image: node:16

cache:
  paths:
    - node_modules/

pages:
  stage: deploy
  script:
    - npm install
    - hexo config theme_config.google_analytics UA-58200086-6
    - npm run build
  artifacts:
    paths:
      - public
  only:
    - master
  environment:
    name: production
    url: https://blog.longtian.info
  
